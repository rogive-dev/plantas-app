export class Planta {
  nombre: string;
  tipo: string;
  clima: string;
 
  public constructor(nombre: string, tipo: string, clima: string) {
    this.nombre = nombre;
    this.tipo = tipo;
    this.clima = clima;
  }
 }